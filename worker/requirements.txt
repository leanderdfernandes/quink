fastapi==0.115.6
uvicorn[standard]==0.34.0
google-genai==1.33.0
ffmpeg-python==0.2.0
# 2.31.0+ — earlier versions reject the modern `sb_secret_...` / `sb_publishable_...`
# API keys at client construction: they regex-validate the key as a legacy JWT.
supabase==2.31.0
pydantic==2.10.4
python-dotenv==1.0.1
python-multipart==0.0.20
# Only used by the real (non-stub) custom-domain verifier's CNAME lookup.
dnspython==2.7.0
