The bolt from the wordmark, on its own and in currentColor — a progress marker, a bullet, or a small brand accent.

```jsx
<span style={{ color: 'var(--brand)' }}><Bolt height={14} /></span>
```

Used in the source on the build bar's leading edge while a guide generates. It is not a logo substitute — for identification use `<Wordmark>`.
