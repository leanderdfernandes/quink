Small mono metadata. This replaced v1's brand-coloured uppercase eyebrow.

```jsx
<Micro>Steps</Micro>
<Micro as="span">8 steps · updated 12 Mar</Micro>
```

Keep it muted (`--ink-3`/`--ink-4`). If a label needs to shout, the headline above it isn't doing its job.
