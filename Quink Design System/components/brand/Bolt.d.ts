/** The lightning-bolt mark alone, inheriting currentColor. */
export interface BoltProps {
  /** Rendered height in px. 11 inline, up to 40 as a standalone mark. @default 11 */
  height?: number;
  style?: React.CSSProperties;
}
export declare function Bolt(props: BoltProps): JSX.Element;
