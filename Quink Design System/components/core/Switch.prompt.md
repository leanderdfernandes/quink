Immediate on/off, right-aligned in a menu row or settings line.

```jsx
<Switch checked={hidden} onChange={setHidden} label="Hide from search and browsing" />
```

Always followed by a `small` line naming the consequence, as the publish menu does.
