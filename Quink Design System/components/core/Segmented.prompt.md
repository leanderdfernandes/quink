A mode switch for two or three exclusive views.

```jsx
<Segmented options={['Edit', 'Preview']} value={mode} onChange={setMode} />
```

The thumb animates on a spring; keep labels short so it doesn't have far to travel. Above three options use a rail.
