Text entry. Placeholders are specific examples, never restated labels.

```jsx
<Input placeholder="e.g. Connecting a Postgres read replica and running the first sync" />
<Input search placeholder="Search your articles…" />
```

Focus lifts the field to the raised surface and draws an inset ring. Do not add a border or an outline.
