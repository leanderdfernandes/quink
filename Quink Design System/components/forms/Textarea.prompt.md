Longer free text — product description, "what does this recording show?".

```jsx
<Textarea maxLength={600} placeholder="What does your product do?" />
```

State any cap in the Field hint, as a number.
