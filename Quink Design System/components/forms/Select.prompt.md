A short closed list — audience, tone, folder.

```jsx
<Select options={['New users','Existing customers','Internal team','Admins']} defaultValue="New users" />
```

Always ship a default selected so the form can be submitted untouched.
