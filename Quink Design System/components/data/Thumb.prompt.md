```jsx
<Thumb src={shot} active />
<Thumb index={5} />   {/* no frame captured yet */}
```

Never skip the entry when there's no image — a missing screenshot is a visible state the user needs to see ("Screenshot coming").
