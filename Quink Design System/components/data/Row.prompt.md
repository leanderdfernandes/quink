The unit of every list in Quink.

```jsx
<Row title="Connect a Postgres read replica" desc="Point Northwind at a replica so syncs never touch production."
     state={<State state="live" />} meta={<Micro as="span">8 steps · 12 Mar</Micro>} href="#" />
```

Rows go inside `<Group>`, not inside `<Card>`. Actions fade in on hover and are pinned visible on touch pointers.
