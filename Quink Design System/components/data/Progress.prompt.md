```jsx
<Progress value={stageIndex / 4} />
```

Pair it with the stage LABEL ("Detecting each action"), never with a percentage — the label is what makes the wait feel accounted for. Progress must reflect real stages; a timer-driven fake is explicitly against the product's rules.
